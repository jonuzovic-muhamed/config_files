# Environment import is handled by dbus-update-activation-environment in autoexec.conf
# Portals are D-Bus activated automatically; explicit start only if needed.
exec systemctl --user start xdg-desktop-portal xdg-desktop-portal-wlr xdg-desktop-portal-gtk
